import { sampleProvider } from "./sample";
import type { OddsProvider } from "./types";

// Switch to OddsApiProvider once you have an API key wired up.
export const oddsProvider: OddsProvider = sampleProvider;
